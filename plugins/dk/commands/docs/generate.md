---
description: Post-release doc-sync, scoped to this milestone's delta.
gate: always
---
**One paste at a time, in this order.** `document-generate` and `code-documenter` are
**sequential, not alternatives**; running only one leaves half the doc set unwritten.

Use the document-generate skill in its post-release doc-sync mode, scoped to this milestone's
delta, not the whole doc surface.

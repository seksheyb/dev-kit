{
  "asset": "code-review-gate",
  "verdicts": [
    {
      "claim_id": "code-review-gate-1",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:28 — \"Inputs you receive from the orchestrator in round mode:\" (followed by lines 29-31: `phase_dir`, `round` — integer, `1..6`, `branch` — the phase's integration branch name (used for `git diff` context))",
      "reasoning": "Verified: line 28 says exactly what the claim says, and the agent explicitly declares these three as values it RECEIVES from the orchestrator. KICKOFF line 701-702 supplies the actual argument values (PHASE/, the loop counter n, <branch>) plus the mode selection between the two modes the agent itself offers (line 21 vs line 26). None of that is derivable by the agent. Load-bearing; keep."
    },
    {
      "claim_id": "code-review-gate-2",
      "confirmed": false,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:45 — \"1. Determine your mode's default engine (single → `claude`; round → `codex`), or use the caller-supplied `engine` override.\"; line 46 — \"2. Run that engine's availability check per `@references/independent-review.md`. Fall back per the registry's order if unavailable (`claude` terminates every fallback chain).\"",
      "reasoning": "REFUTED. The quoted skill sentence at line 37 does exist verbatim, but the classification fails. `engine` is a caller-supplied override the agent explicitly solicits at line 45 — selection among engines the skill itself offers, which the discipline classes as load-bearing, not duplication. The claim's stated harm is also factually wrong: it asserts an explicit override \"reads as a pin\" and \"weakens the fallback chain\", but line 46 runs the availability check and falls back per the registry regardless of whether the engine came from the default or from a caller override, and references/independent-review.md:45-48 says the same. So `(engine codex)` neither pins nor degrades anything. A two-word explicit argument is not a restatement of the skill's methodology."
    },
    {
      "claim_id": "code-review-gate-3",
      "confirmed": false,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:28-31 — \"Inputs you receive from the orchestrator in round mode: ... `branch` — the phase's integration branch name (used for `git diff` context)\" (no optional marker); agents/code-review-gate.md:468 — \"- [ ] Review scope determined explicitly (file list, diff base, or sprint branch — never guessed)\"",
      "reasoning": "REFUTED as an unenforced-gap. The claim's search evidence is accurate as far as it goes — I re-ran the greps and `fail closed` appears only at line 317 under the heading \"**3. Determine changed files (single mode):**\" — but the gap does not follow. The KICKOFF sentence asserts a duty on the CALLER (supply the argument), not behavior of the agent (\"abort if branch is missing\"). No skill text can bind its caller to pass an argument, so this is ordinary argument-passing emphasis, i.e. load-bearing-parameter. And the agent is not silent on the substance either: line 28 lists `branch` flatly among the inputs it receives in round mode with no optional marker, and the success criterion at line 468 requires review scope — explicitly including \"sprint branch\" — to be determined explicitly and \"never guessed\". Synonym coverage exists, so the absence claim fails. (Side note, not a finding on the guide: adding an explicit round-mode fail-closed rule mirroring line 317 would still be a reasonable hardening of the agent.)"
    },
    {
      "claim_id": "code-review-gate-4",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:90 — \"4. Run `git diff main...<branch>` (or have the dispatched engine run it) to see what changed in this sprint.\"",
      "reasoning": "Verified verbatim at line 90, inside `<round_mode_mechanics>`. KICKOFF 703-704 restates the agent's own round-mode mechanic with the clause order flipped (\"to see what this sprint changed\" vs \"to see what changed in this sprint\"), and line 323 repeats it a second time in the execution flow (\"Round mode instead runs `git diff main...<branch>`\"). Deleting the rationale clause changes no behavior — the agent diffs the branch either way — while leaving it in place creates a drift surface if the agent's diff base ever changes. Severity downgraded from medium to low: it is a single subordinate clause attached to a genuinely load-bearing instruction (claim 3's \"the branch is not optional\"), which is the least harmful form of duplication."
    },
    {
      "claim_id": "code-review-gate-5",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:461 — \"**Round mode output paths must be exactly** `{phase_dir}/reviews/round-<round>/findings.md` and `.../findings.json`.\"; also pinned at lines 33-35 (\"Output paths (you create the round directory)\") and line 358.",
      "reasoning": "Verified verbatim at line 461 under `<critical_rules>`. This is the one place where the \"output paths are the invocation's arguments\" exemption does NOT apply: the caller does not supply this path, the agent derives it from `phase_dir` and pins it as a hard rule in three separate places. Restating it in the prompt is describing the agent's fixed output contract back to it. Severity lowered from medium to low because the sentence does real antecedent work for the fixes.json path in item 3 (\"the same round directory code-review-gate just wrote findings.json into\") — the literal path string is the duplicated part, not the wiring."
    },
    {
      "claim_id": "code-review-gate-6",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:99 — \"8. Compute / verify `stop_loop`: `true` iff `counts.P0 == 0 AND counts.P1 == 0 AND previously_seen_classes is empty (or every class listed there is now resolved per a prior round's fixes.json)`. Otherwise `false`.\"",
      "reasoning": "Verified verbatim at line 99. The agent computes `stop_loop`/`next_action` but has no reach into its caller's control flow; \"read those, never re-derive them\" is a constraint on the orchestrator that no agent text can enforce. Load-bearing; keep."
    },
    {
      "claim_id": "code-review-gate-7",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "agents/code-review-gate.md:37 — \"Default engine: `codex`. Do not run round-mode reviews as `claude` by default — the point of the loop is a second, structurally independent pass; but `claude` remains available if no external engine is installed (see the registry's fallback order).\"",
      "reasoning": "Verified verbatim at line 37, including the shared phrase \"structurally independent pass\". Also carried by references/independent-review.md:42 (\"Adversarial review rounds (`code-review-gate`, round mode) | `codex` | `codex` → `gemini` → `claude`\") and by the agent's own frontmatter description at line 3 (\"round mode, default engine `codex`\"). The guide restates both the rule and its justification; the agent defaults round mode to codex without being told, so the sentence changes no behavior. Severity lowered from high to medium: it is ~1.5 lines of prose, not a block of methodology, and it is a duplication rather than a contradiction."
    },
    {
      "claim_id": "code-review-gate-8",
      "confirmed": false,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:46 — \"2. Run that engine's availability check per `@references/independent-review.md`. Fall back per the registry's order if unavailable (`claude` terminates every fallback chain).\"; line 47 — \"3. Record which engine actually ran in your output (`engine` field in `findings.json`) — a fallback must be visible to the caller, never silent.\"",
      "reasoning": "REFUTED as scoped. Both skill lines are verbatim-accurate. But the claim quotes and classifies the WHOLE sentence (KICKOFF 707-709) as redundant-restatement, and the sentence's operative instruction is \"say so in the final report, because a claude-on-claude round is a weaker gate\" — a duty on the operator's human-facing summary, which line 47 does not reach (it puts the engine in `findings.json`, a machine field the agent returns, not the operator's report). The fallback clause is not freely deletable either: it is the grammatical subject of \"say so\". Delete it and the instruction breaks, which is the test for load-bearing. The claim's own remedy (\"the sentence should shrink, not vanish\") concedes this; a duplication finding over a sentence whose instruction must survive is the wrong classification."
    },
    {
      "claim_id": "code-review-gate-9",
      "confirmed": false,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:100-101 — \"9. Compute / verify `next_action`:\" / \"   - If `stop_loop`: `\\\"stop loop — clean exit\\\"`.\"",
      "reasoning": "REFUTED. Line 101 exists verbatim, but it defines a STRING the agent writes into `findings.json` — it is not, and cannot be, an instruction that exits the orchestrator's loop. KICKOFF item 2 (\"If stop_loop is true, leave the loop\") is the exit branch of a loop the guide itself is defining (\"Then loop, round n = 1..6:\"); delete it and the workflow has no termination path at all. Caller-side control flow is the guide's own content, not the agent's methodology. The claim's supporting argument — that the guide contradicts its earlier \"never re-derive them\" — misreads that instruction: reading `stop_loop` and branching on it IS reading it, not re-deriving it (re-deriving would mean recomputing it from P0/P1 counts). The duplication with lines 744-745 is guide-internal, which is outside this audit's guide-vs-skill scope."
    },
    {
      "claim_id": "code-review-gate-10",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:88 — \"2. Glob `{phase_dir}/reviews/round-*/findings.json` for prior rounds. Also glob `{phase_dir}/reviews/round-*/fixes.json`. These are inputs the engine needs for `previously_seen_classes` detection.\"; line 99 supplies the \"(or every class listed there is now resolved per a prior round's fixes.json)\" arm of the stop_loop rule.",
      "reasoning": "Verified verbatim at lines 88 and 99, and the guide's derivation is accurate: with no fixes.json on disk the \"or resolved\" arm of line 99 is unavailable, so stop_loop needs a genuinely empty `previously_seen_classes`. That accuracy is exactly the problem — it is the agent's internal glob and stop_loop rule spelled out in the prompt, which is what the drift-prone class covers. Delete the rationale and the load-bearing instruction (\"That path is required and it is not a free choice\", plus the fixes.json path handed to bugfix-wave) survives intact. Kept at low severity for the reason the claim gives: without some justification the mandatory path reads as a free choice, so the fix is to compress the rationale, not to cut it."
    },
    {
      "claim_id": "code-review-gate-11",
      "confirmed": false,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:103 — \"   - Else (`round == 6` and `stop_loop` false): `\\\"hard cap reached — escalate\\\"`.\"; line 26 — \"**Round mode** (`round` input given, `1..6`)\"",
      "reasoning": "REFUTED, on the same ground as claim 9. Line 103 is verbatim-accurate but it only defines the `next_action` string the agent writes; the agent cannot stop its caller from dispatching a 7th round, and line 26's `1..6` is an input declaration, not a bound on the orchestrator. The KICKOFF sentence is the loop's termination rule — caller-side control flow the guide must own. The only genuine overlap is the quoted escalation string, and quoting it lets the operator recognize the value they will read back out of findings.json, which is useful rather than redundant. The remaining overlap is with the guide's own item 2, i.e. guide-internal repetition, not guide-vs-skill duplication."
    },
    {
      "claim_id": "code-review-gate-12",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "agents/code-review-gate.md:21 — \"**Single mode** (default — no `round` input given): one-shot review of an explicit file list, a phase's diff, or a branch about to land.\"",
      "reasoning": "Verified verbatim at line 21. The parenthetical is conditional gating the operator must judge (\"only if you want a one-shot look\") plus a cross-step relationship — that /dev-kit-core:review does not satisfy round 1 of the loop — which the agent cannot know, since nothing in the agent references the loop's round 1 as a substitutable step. Verified against commands/review.md:7, which dispatches \"`agents/code-review-gate` (single mode)\", so the guide's description is accurate with no drift. Load-bearing; keep."
    }
  ]
}

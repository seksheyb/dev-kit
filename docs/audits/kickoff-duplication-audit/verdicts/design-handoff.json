{
  "asset": "design-handoff",
  "verdicts": [
    {
      "claim_id": "design-handoff-1",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "SKILL.md:34-35 — \"1. Locate the source documents: **DESIGN.md** (`docs/global/design/DESIGN.md`), plus this / phase's **UI-SPEC.md** (`docs/milestones/<M>/phases/<NN>-<slug>/UI-SPEC.md`) if present.\" The skill's only self-gating is at 34-37 (\"If no DESIGN.md exists, stop and suggest running design-consultation first\") and at 29-30 (\"typically Stage 8, per phase, per screen\"). Nothing in the 113-line skill (no references/ dir exists — the skill is a single file) conditions invocation on whether a phase ships UI.",
      "reasoning": "Verified the KICKOFF text myself: the italic conditioner is at KICKOFF.md line 635, not 636 as the claim states (636 is the opening ```text fence) — a citation nit, not a substance failure; the string exists verbatim and means what the claim says. Substantively the claim holds: whether a given phase ships UI at all is a per-phase judgment the orchestrator makes before dispatch, and the skill's nearest analogue (stop if DESIGN.md is absent) is a different condition — DESIGN.md is global and exists project-wide even for non-UI phases, so the skill's guard would NOT prevent a wasteful invocation on a backend-only phase. Explicitly listed in the rubric as load-bearing conditional gating. Keep."
    },
    {
      "claim_id": "design-handoff-2",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "SKILL.md:3-6 (frontmatter description) — \"Bridge a Claude Design system into codebase-native implementation: translate a project's / DESIGN.md (and UI-SPEC, if present) into a compressed hex/typography quick-reference and / ready-to-use component prompts for whatever's building the *actual codebase*\"; plus SKILL.md:34-35 which fixes the literal input path `docs/global/design/DESIGN.md` and the UI-SPEC \"if present\" condition, and SKILL.md:68-69 which fixes the \"Quick Color Reference\" / \"Typography Reference\" table output shape.",
      "reasoning": "Confirmed, but with the scope narrowed and severity cut from medium to low. Every substantive element of the descriptive tail is already fixed by the skill: the literal DESIGN.md path (line 34), the UI-SPEC-if-present condition (line 35), and the hex/typography-reference + component-prompts output shape (frontmatter line 4-6, restated at 68-72). None of it is an operator-supplied argument. However, a hostile reader is right that the quoted span cannot be deleted wholesale: the leading \"Then use the design-handoff skill\" carries an ordering constraint relative to design-html in the same block, which is load-bearing sequencing the skill cannot know. Only the tail (\"to translate docs/global/design/DESIGN.md, plus PHASE/UI-SPEC.md if this phase has one, into a hex/typography quick-reference and copy-paste component prompts for the codebase-side implementer\") is redundant; the invocation-plus-ordering clause must stay. Low severity accordingly."
    },
    {
      "claim_id": "design-handoff-3",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "SKILL.md:23-28 — \"**Why this exists, specifically:** anything that stays inside Claude Design (`design-html`, / Variant Shotgun) already gets the bound system's tokens/context loaded automatically via / `get_claude_design_prompt(design_system_id: ...)` — it doesn't need this skill at all. The gap / this skill closes is the *other* side: once a screen is built in Claude Design and needs to be / translated into the project's real frontend framework, that implementer has no automatic access / to the Claude Design system's context.\" Also SKILL.md:96-99 restates it (\"Not `design-html` — it doesn't need this, since Claude Design already loads its own system context automatically.\").",
      "reasoning": "Verified both halves. KICKOFF 642-644 reproduces the skill's rationale with the same two clauses in the same order — implementer-has-no-access, and context-loads-automatically-only-inside-Claude-Design — with `react-expert` as the concrete stand-in for the skill's own `react-expert`/`vue-expert` list at line 19. This is pure internal justification that the skill documents twice (23-28 and 96-99); it supplies no path, parameter, condition, or ordering. Fully removable prose. Downgraded from high to medium: it is 3 of the block's 13 lines and wholly redundant, but it is explanatory only and carries no behavioral rule that could drift into a contradiction."
    },
    {
      "claim_id": "design-handoff-4",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "high",
      "evidence": "SKILL.md:83-85 — \"Save the output to `docs/global/design/handoffs/<name>.md`, where `<name>` is the project or / screen name — this local file is the primary artifact, since the codebase-side implementer / reads it directly and has no Claude Design MCP access of its own.\"",
      "reasoning": "Confirmed, and I deliberately tested the rubric's carve-out that output paths are the invocation's arguments. That carve-out does not apply here: the skill hard-codes this exact path AND the `<name>` substitution rule AND the justifying clause in its Deliverable section, so KICKOFF is not supplying an argument, it is transcribing the skill's own fixed output convention — down to the em-dash clause. High severity is warranted because measurable drift has already begun: KICKOFF says \"no Claude Design access\" where the skill says \"no Claude Design MCP access,\" and KICKOFF's copy silently drops the skill's conditional archival rule at 85-88 (write_files a copy back into the Claude Design project when DESIGN.md carries a `claude_design_project_id` comment), so an operator reading only KICKOFF gets a subtly incomplete Deliverable spec. Delete the sentence; the skill owns it."
    },
    {
      "claim_id": "design-handoff-5",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "SKILL.md:96-99 — \"Suggest next steps: hand the instructions file to whatever is building the actual codebase — / the relevant lane skill (`react-expert`, `vue-expert`, `nextjs-developer`, etc.), a general / frontend developer agent, or the human doing it by hand. Not `design-html` — it doesn't need / this, since Claude Design already loads its own system context automatically.\"",
      "reasoning": "Confirmed. The overlap is partial and the residue is genuinely load-bearing. The skill offers handing the file to a builder only as a *suggested next step* to the user (\"Suggest next steps:\"), addressed to an unspecified \"whatever is building the actual codebase.\" KICKOFF converts that suggestion into an orchestration requirement with two things the skill cannot know: the concrete recipients in this stage's fan-out (\"the UI tracks\" — a stage-8 parallel-track concept absent from the skill; grepped, the skill never uses the word \"track\"), and the ordering constraint \"before they build.\" Cross-skill ordering and recipient selection are explicitly load-bearing per the rubric. Low severity because the generic hand-it-off notion is duplicated; only the recipients and the sequencing are new."
    },
    {
      "claim_id": "design-handoff-6",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "SKILL.md:49-50 — \"Extract and summarize each of these. If a section is missing from the source, flag the gap / explicitly — never guess or fill in values:\". Reinforced three more times: SKILL.md:107 \"- Flag every gap explicitly\", SKILL.md:110 \"- Skip sections — a missing section is a flagged gap, not an omission\", SKILL.md:112 \"- Guess missing info — recommend defaults only in the clearly-marked Gaps section\". A Gaps output section is also mandated at SKILL.md:77-79.",
      "reasoning": "Confirmed. Both halves of the KICKOFF clause map one-to-one onto skill line 49-50: \"flag any section missing from the source\" = \"If a section is missing from the source, flag the gap explicitly\"; \"rather than inventing values\" = \"never guess or fill in values.\" This is not incidental phrasing — it is the skill's single hardest extraction rule, stated four separate times plus given a dedicated output section. Nothing in the KICKOFF phrasing narrows, conditions, or parameterizes it. Medium rather than high: it is one clause, and re-emphasis of a hard rule is low-risk, but it supplies zero information the skill lacks."
    }
  ]
}

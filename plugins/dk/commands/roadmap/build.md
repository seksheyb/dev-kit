---
description: Four-axis ecosystem research, synthesis, then split the milestone into phases.
argument-hint: "[M]"
gate: always
---
Use the context-restore skill. Then dispatch four project-researcher agents in one message,
research_mode ecosystem, one per assigned_axis: stack, features, architecture, pitfalls — same
context and open questions to each. When all four return, the research-synthesizer agent to merge
them, then the roadmapper agent to split milestone <M> into phases.

**Session boundary.**

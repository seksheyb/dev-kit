---
description: Phase research, one advisor-researcher per gray area, then pattern mapping.
argument-hint: "[NN]"
gate: always
---
Use the phase-researcher agent for phase <NN>, then fan out one advisor-researcher agent per
gray-area decision in one message, each given its gray area, the phase and project context, and
the same calibration tier; fold their tables in yourself. Then the pattern-mapper agent for phase <NN>.

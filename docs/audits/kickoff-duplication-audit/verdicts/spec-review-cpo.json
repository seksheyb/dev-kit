{
  "asset": "spec-review-cpo",
  "verdicts": [
    {
      "claim_id": "spec-review-cpo-1",
      "confirmed": false,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "SKILL.md:29-30 — \"This lens owns strategy/scope, and it is the **only** place in the pipeline where scope and product value are decided.\" (KICKOFF.md:175 — \"**The scope gate — the only product/strategy gate in the pipeline:**\", both verified verbatim)",
      "reasoning": "REFUTED. The semantic overlap with SKILL.md:29-35 is real, but KICKOFF line 175 sits OUTSIDE the ```text``` prompt block (block runs 177-183), so it is never pasted into the agent's context and cannot duplicate an instruction to the agent. It is the guide's navigational lead-in for a human operator who has not read the skill, and it carries cross-step positioning — that of all ~10 pipeline steps only this one decides scope — which is guide-level information the guide reader cannot get from any single skill. Deleting it would leave the operator with an unlabelled prompt block. Reclassified as load-bearing operator orientation; keep."
    },
    {
      "claim_id": "spec-review-cpo-2",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "SKILL.md:19-20 — \"Review only, against `docs/milestones/<M>/specs/<NNN>-<slug>/spec.md` (WHAT/WHY), never HOW.\"",
      "reasoning": "CONFIRMED as load-bearing (keep). Verified KICKOFF.md:16 defines the shorthand: \"`SPEC/` = `docs/milestones/<M>/specs/<NNN>-<slug>/`\", so `SPEC/spec.md` resolves to exactly the skill's own stated review target — no contradiction, no drift. Naming the skill and its target artifact is the invocation's irreducible core: without it there is no invocation. Not duplication."
    },
    {
      "claim_id": "spec-review-cpo-3",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "SKILL.md:43 — \"Pick one posture and commit to it. Do not drift.\"; SKILL.md:139 — \"## Step 0: Nuclear Scope Challenge\"; SKILL.md:141 — \"### 0A. Premise Challenge\"",
      "reasoning": "CONFIRMED. Both clauses restate mandatory internal phases. \"Challenge the premise\" = Step 0 / 0A, which the skill runs unconditionally (line 141, with its three questions at 142-146). \"Commit to a scope posture\" is verbatim the rule at line 43, and the skill picks the posture itself from the context-dependent defaults at lines 56-59 — reinforced by line 23 (\"Pick the posture from the context-dependent defaults below, state it in the report\"). Checked the mode-selection carve-out and it does not apply: the KICKOFF names no specific posture, so the operator is not selecting among the skill's four modes, merely telling it to do what it already must."
    },
    {
      "claim_id": "spec-review-cpo-4",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "SKILL.md:100-102 — \"**Story-bank alignment (if `US-xxx` Theme→Pillar hierarchy exists):** Does this spec's prioritization match its Pillar's stated intent? A HOLD/REDUCTION posture on a Pillar's flagship story is a finding worth surfacing explicitly.\"; also SKILL.md:80 — \"This is the core of what this lens owns\" (Product Prioritization Judgment, 78-117)",
      "reasoning": "CONFIRMED but downgraded to low. Both halves are skill-owned: prioritization scoring is the section the skill calls \"the core of what this lens owns\" (RICE/Kano/JTBD/North Star, lines 83-94, unconditional), and the `US-xxx` Theme→Pillar taxonomy is named at line 100 and carried again in the backlog template at line 224. The only non-redundant residue is that the KICKOFF asserts flatly what the skill guards with \"if ... exists\" — but the skill self-detects by reading the spec, so the assertion buys nothing. Severity lowered from medium: it is a five-word clause whose marginal cost is small."
    },
    {
      "claim_id": "spec-review-cpo-5",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "SKILL.md:239-241 — \"Write this as a `## CPO Review` section appended to the spec file (same append-in-place pattern `specify`'s Clarification Pass uses for its `## Clarifications` section — never overwrite existing spec content):\"; SKILL.md:258 — \"**LOCK:** This Scope Decision Record is binding — this spec's scope does not get re-litigated downstream…\"",
      "reasoning": "CONFIRMED, with a correction: the quoted sentence is at line 239, not 238 (line 237 is the heading \"## Required Outputs — the Scope Decision Record\", 238 is blank). Content verified. The skill's template fixes the artifact name (Scope Decision Record), the section heading (`## CPO Review`), the append-in-place behavior, and the mandatory LOCK line at 258 — all three things the KICKOFF clause names. No output path or filename is supplied that the skill lacks, so this is output-format restatement, the canonical duplication signature, not an invocation argument."
    },
    {
      "claim_id": "spec-review-cpo-6",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "high",
      "evidence": "SKILL.md:110-114 — \"**This is not just a recommendation — descoped items get written to the backlog, not just mentioned in prose.** Whatever this milestone doesn't ship gets removed from `spec.md`'s active scope and appended to `docs/global/requirements/BACKLOG.md` (create if it doesn't exist yet) as real, findable entries for whichever future milestone picks them up — see \\\"Backlog Handoff\\\" below.\"",
      "reasoning": "CONFIRMED. Near-verbatim echo, including the same emphatic \"not just … prose\" contrast and the identical literal path `docs/global/requirements/BACKLOG.md`. \"ID-tracked entries\" is covered too: the Backlog Handoff section (209-235) fixes the `BL-<NNN>` scheme, the entry template (217-228), and the counter discipline at line 230 (\"`BL-<NNN>` is a flat, global, never-reused counter\"). Nothing in the KICKOFF sentence is an argument, a path the skill lacks, or a condition the operator must judge — the skill already emphasizes this harder than the guide does. Highest-severity item on this asset: it is the longest clause and the most literal duplication, including the path."
    },
    {
      "claim_id": "spec-review-cpo-7",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "SKILL.md:127-129 — \"Read the spec itself, the constitution (if one exists — any conflict with a MUST principle is an automatic BLOCKER, same as `analyze`'s rule), and `docs/milestones/<M>/research/MARKET.md` if `market-researcher` has already run\"",
      "reasoning": "CONFIRMED. Verified at line 127; the skill states the identical hard gate in almost identical words (\"any conflict with a MUST principle is an automatic BLOCKER\"), and it is the skill that both loads the constitution and owns the severity taxonomy where BLOCKER lives (line 269). Re-deriving a gate the skill already enforces unconditionally. The KICKOFF drops the skill's \"(if one exists)\" guard but the skill self-detects, so no load-bearing condition is added."
    },
    {
      "claim_id": "spec-review-cpo-8",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "SKILL.md:272-273 — \"**Verdict:** APPROVE / APPROVE-WITH-CHANGES / REVISE. A premise failure (0A) is always at least one BLOCKER — the spec should not proceed to `writing-plans`/`planner` until resolved.\"",
      "reasoning": "CONFIRMED as load-bearing (keep). Verified at KICKOFF.md:185, outside the prompt block — it is an instruction to the human operator, not to the agent, and an agent-facing skill cannot enforce operator behavior at all. The load-bearing content is pipeline mapping the skill cannot know: that the downstream consumer here is KICKOFF's numbered \"step 2\" (Architecture & tech stack), and that the halt applies to the whole pipeline advance, not merely to `writing-plans`/`planner`. Cross-step ordering/gating, not restatement."
    }
  ]
}

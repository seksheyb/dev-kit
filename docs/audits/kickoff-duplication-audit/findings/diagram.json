{
  "asset": "diagram",
  "asset_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/skills/diagram/SKILL.md",
  "references_read": [],
  "claims": [
    {
      "local_id": "diagram-1",
      "class": "redundant-restatement",
      "severity": "medium",
      "step": "02",
      "kickoff_line_range": [203, 205],
      "kickoff_quote": "its artifact contract is the editable `.mmd` source plus rendered `.svg`/`.png` (optionally `.excalidraw`), committed as a pair, so don't hand-draw or paste a flattened image in its place.",
      "skill_quote": "Every run emits an **artifact set**, never a dead pixel dump:",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/skills/diagram/SKILL.md",
      "skill_line": 8,
      "reasoning": "The skill's opening line plus the artifact table immediately under it (lines 10-14) already fix exactly this contract: `<slug>.mmd` as editable source, `<slug>.svg` + `<slug>.png` rendered, `<slug>.excalidraw` optional and flowchart-only. The 'don't paste a flattened image' half is the skill's own hard rule at line 55 ('**Never ship without rendering**... A `.mmd` file alone is not a diagram.') plus line 50 ('Never edit the SVG/PNG directly — they are derived artifacts'). The operator prompt is re-teaching the skill's own output format.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "diagram-2",
      "class": "unenforced-gap",
      "severity": "medium",
      "step": "02",
      "kickoff_line_range": [205, 209],
      "kickoff_quote": "Keep that pair current as the design moves, and keep the Mermaid embedded in the SDD in sync with it: step 14's drift check never opens `.mmd` or `.svg` at all — it extracts entity names from the ASCII and Mermaid blocks inside the markdown docs and cross-references them against the diff, and it only reports advisory findings. Nothing downstream catches a stale source file for you.",
      "skill_quote": "CLOSEST MATCH FOUND (not coverage): line 56 — '- For diagrams destined for a markdown doc or PDF pipeline that renders ` ```mermaid ` fences natively, embed the mermaid source rather than the PNG — it stays diffable and editable.' This tells the author to embed INSTEAD of the PNG; it never contemplates an embedded copy coexisting with a separate `.mmd` file, nor any duty to re-sync them. Greps run over the whole 61-line SKILL.md (no `references/` subdirectory exists — `ls` of the skill directory returns only SKILL.md): `grep -niE 'sync|stale|current|embed|drift|doc |commit'` returned only line 24 ('the current working directory when it's a git repo (artifacts the user can commit alongside their existing files)') and line 56. No hit for 'sync', 'stale', or 'drift' anywhere in the file. The nearest behavioral rule, line 50 ('**Iteration loop:** when the user wants changes to the diagram's content or structure, edit the `.mmd` source and re-run Step 2.'), is user-request-triggered re-rendering, not an ongoing obligation to keep a doc-embedded duplicate consistent with the on-disk source.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/skills/diagram/SKILL.md",
      "skill_line": 56,
      "reasoning": "The skill has no concept of a diagram being duplicated into a host document and later drifting from its `.mmd` source; its one relevant rule chooses embed-vs-PNG at authoring time and stops there. The KICKOFF text is therefore load-bearing and must be KEPT — but the underlying maintenance duty is unenforced by the skill. The step-14 clause explaining what the downstream drift check does NOT do is genuinely outside the skill's knowledge and is load-bearing on its own terms.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "diagram-3",
      "class": "load-bearing-parameter",
      "severity": "low",
      "step": "02",
      "kickoff_line_range": [202, 203],
      "kickoff_quote": "Use the\ndiagram skill for the architecture diagrams",
      "skill_quote": "Write mermaid for the user's request.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/skills/diagram/SKILL.md",
      "skill_line": 18,
      "reasoning": "The skill draws whatever subject it is handed; naming 'the architecture diagrams' as the subject is the invocation's argument and cannot live in the skill. Keep.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "diagram-4",
      "class": "load-bearing-parameter",
      "severity": "low",
      "step": "07",
      "kickoff_line_range": [495, 496],
      "kickoff_quote": "Use the\ndiagram skill for the wave/track dependency graph.",
      "skill_quote": "Write mermaid for the user's request.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/skills/diagram/SKILL.md",
      "skill_line": 18,
      "reasoning": "Same shape as diagram-3: the subject of the diagram is the invocation argument. Note that a dependency graph is a flowchart, the one type the skill calls its 'sweet spot' (line 20) and the only type supporting `.excalidraw`, so nothing extra needs saying here.",
      "systemic": false,
      "other_locations": []
    }
  ],
  "consistency": "No contradiction between the two invocations, but the depth is asymmetric: step 02 spells out the full artifact contract (.mmd + .svg/.png + optional .excalidraw, committed as a pair, keep current) while step 07 is a bare one-liner with no artifact expectation at all. If the step-02 restatement is treated as necessary, step 07 is under-specified; if step 07's bare form is sufficient (it is — the skill fixes the contract itself), that is independent evidence that the step-02 elaboration is the redundant one. Recommend collapsing step 02 to the step-07 form plus the genuinely load-bearing SDD-sync / step-14 sentence.",
  "capped": ""
}

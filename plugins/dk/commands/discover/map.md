---
description: Discovery wave 1 — four codebase-mappers, the assumptions-analyzer, and the phase-researcher.
argument-hint: "[NN]"
gate: always
---
Use the context-restore skill. Then dispatch six agents in one message for phase <NN>: four
codebase-mapper agents, one focus area each — tech, arch, quality, concerns — plus one
assumptions-analyzer and one phase-researcher, the latter two given the calibration tier from
CLAUDE.md's Discovery Calibration section (`standard` when that section is missing or blank).
Give the phase-researcher the roadmap and the phase description only, never the codebase map:
the mappers are rewriting it in this same wave, and it would read a half-written map. These six
fan out safely; fold the assumptions and the phase-relevant map findings in yourself.

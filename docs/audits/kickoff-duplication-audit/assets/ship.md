# ship

- **kind**: skill
- **file**: `/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/skills/ship/SKILL.md`
- **file_lines**: 218
- **has_references**: no
- **complexity**: medium
- **invocation_count**: 1
- **invoked by steps**: 13

---

## Invocation 1 — step 13 (Ship — open the PR), block 4, lines 1166-1173

### verbatim_text

```text
Use the ship skill for this milestone. It is non-interactive: merge base → test-failure triage
→ coverage-gap tracing, reusing each phase's nyquist-auditor results as pre-confirmed evidence
rather than re-tracing every path → plan-completion audit, reusing PHASE/VERIFICATION.md and
converge's output the same way → adversarial pre-landing review scoped only to the delta ship
itself just generated → version bump → CHANGELOG → bisectable commits → push →
create/update the PR. End at the PR URL. Do not merge and do not deploy — land-and-deploy is a
separate skill that runs after step 14, not something you call from here.
```

### surrounding_prose

Labeled 'Automated:' — the alternative path to the 'Manual:' block above (block_index 3); the operator picks one path, not both. Mentions nyquist-auditor, converge, and land-and-deploy only as things the ship skill reuses or explicitly does NOT call — these are referenced, not invoked directly by this prompt block.

---

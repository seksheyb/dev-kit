---
description: Pick and enable the dev-kit lane plugins this stack needs.
gate: always
---
From the SDD, tell me which dev-kit lane plugins this stack needs, then enable them and name the
ones you enabled. Step 8 routes each implementation track through these.

**Session boundary.**

→ next: `/clear`, then `/dk:roadmap:build`

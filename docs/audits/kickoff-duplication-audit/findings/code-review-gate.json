{
  "asset": "code-review-gate",
  "asset_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
  "references_read": [
    "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/references/independent-review.md",
    "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/references/review-engines/",
    "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/commands/review.md"
  ],
  "claims": [
    {
      "local_id": "code-review-gate-7",
      "class": "redundant-restatement",
      "severity": "high",
      "step": "10",
      "kickoff_line_range": [706, 707],
      "kickoff_quote": "Round mode defaults to codex so the review is a structurally independent pass rather than the same model grading its own homework.",
      "skill_quote": "Default engine: `codex`. Do not run round-mode reviews as `claude` by default — the point of the loop is a second, structurally independent pass; but `claude` remains available if no external engine is installed (see the registry's fallback order).",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 37,
      "reasoning": "The guide restates the agent's own default-engine rule AND its justification, using the same phrase 'structurally independent pass'. The agent already defaults round mode to codex without being told; this sentence changes no behavior. Also mirrored in references/independent-review.md's per-role default table (line 42).",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-4",
      "class": "redundant-restatement",
      "severity": "medium",
      "step": "10",
      "kickoff_line_range": [703, 704],
      "kickoff_quote": "round mode diffs it to see what this sprint changed, and a round dispatched without it reviews the wrong tree",
      "skill_quote": "4. Run `git diff main...<branch>` (or have the dispatched engine run it) to see what changed in this sprint.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 90,
      "reasoning": "This is the agent's own round-mode mechanic, restated almost word-for-word ('to see what this sprint changed' vs 'to see what changed in this sprint'). The rationale clause teaches the operator internals the agent already carries; only the bare 'branch is not optional' emphasis (claim 3) does any work.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-5",
      "class": "redundant-restatement",
      "severity": "medium",
      "step": "10",
      "kickoff_line_range": [704, 705],
      "kickoff_quote": "It writes\n   PHASE/reviews/round-<n>/findings.{md,json}",
      "skill_quote": "**Round mode output paths must be exactly** `{phase_dir}/reviews/round-<round>/findings.md` and `.../findings.json`.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 461,
      "reasoning": "The round findings path is not an argument the caller supplies — the agent derives it from phase_dir and pins it as a critical rule (also stated at lines 34-35). Restating it in the prompt is describing the skill's fixed output contract back to it. It does serve as the antecedent for the fixes.json path in item 3, so it is not free-standing noise, but the path text itself is duplicated.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-8",
      "class": "redundant-restatement",
      "severity": "medium",
      "step": "10",
      "kickoff_line_range": [707, 709],
      "kickoff_quote": "If no\n   external engine is installed it falls back per the engine registry — say so in the final\n   report, because a claude-on-claude round is a weaker gate.",
      "skill_quote": "2. Run that engine's availability check per `@references/independent-review.md`. Fall back per the registry's order if unavailable (`claude` terminates every fallback chain).",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 46,
      "reasoning": "The fallback-per-registry behavior is the agent's own engine_selection step, and line 47 already mandates 'a fallback must be visible to the caller, never silent' via the findings.json `engine` field. The residual load-bearing sliver is only 'say so in the final report' — surfacing it in the operator's human-facing summary, which the agent cannot reach — so the sentence should shrink, not vanish.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-9",
      "class": "redundant-restatement",
      "severity": "low",
      "step": "10",
      "kickoff_line_range": [723, 725],
      "kickoff_quote": "round n+1's code-review-gate globs PHASE/reviews/round-*/fixes.json to decide whether\n   a previously_seen_class has since been resolved, so with no fixes.json on disk stop_loop can\n   only ever go true on a genuinely empty previously_seen_classes list.",
      "skill_quote": "2. Glob `{phase_dir}/reviews/round-*/findings.json` for prior rounds. Also glob `{phase_dir}/reviews/round-*/fixes.json`. These are inputs the engine needs for `previously_seen_classes` detection.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 88,
      "reasoning": "Both halves are the agent's internals: the glob (line 88) and the stop_loop rule that consults fixes.json (line 99). The constraint being justified — that bugfix-wave must emit to that exact path — is genuinely load-bearing cross-asset wiring, so this is rationale duplication rather than a removable instruction; severity low because deleting the justification would make the path look like a free choice.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-12",
      "class": "redundant-restatement",
      "severity": "low",
      "step": "10",
      "kickoff_line_range": [710, 710],
      "kickoff_quote": "2. If stop_loop is true, leave the loop.",
      "skill_quote": "   - If `stop_loop`: `\"stop loop — clean exit\"`.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 101,
      "reasoning": "The agent already emits next_action = 'stop loop — clean exit' precisely so the caller does not have to re-derive the exit test, and the guide itself told the operator two lines earlier to 'read those, never re-derive them'. The same exit condition is then restated a third time at lines 744-745, so within one prompt the loop-exit rule appears twice on top of the agent's own next_action.",
      "systemic": true,
      "other_locations": [[744, 745]]
    },
    {
      "local_id": "code-review-gate-10",
      "class": "redundant-restatement",
      "severity": "low",
      "step": "10",
      "kickoff_line_range": [744, 745],
      "kickoff_quote": "Leave the loop when stop_loop is true, or at round 6 with stop_loop still false (\"hard cap\nreached — escalate\"). Never open a 7th round.",
      "skill_quote": "   - Else (`round == 6` and `stop_loop` false): `\"hard cap reached — escalate\"`.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 103,
      "reasoning": "The escalation string is quoted verbatim from the agent's next_action ladder, and the round==6 condition is the agent's own. 'Never open a 7th round' is the only caller-side residue, and even that is implied by the agent declaring `round` to be `1..6` (line 26).",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-2",
      "class": "redundant-restatement",
      "severity": "low",
      "step": "10",
      "kickoff_line_range": [702, 702],
      "kickoff_quote": "(engine codex)",
      "skill_quote": "Default engine: `codex`. Do not run round-mode reviews as `claude` by default — the point of the loop is a second, structurally independent pass; but `claude` remains available if no external engine is installed (see the registry's fallback order).",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 37,
      "reasoning": "This is an explicit engine override set to the value the agent already defaults to in round mode. Passing it changes nothing, and it worsens the redundancy because the very next sentences (claim 7) re-explain that codex is the default. It also weakens the fallback chain semantics: an explicit override reads as a pin, whereas the registry expects the default to degrade codex → gemini → claude.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-3",
      "class": "unenforced-gap",
      "severity": "low",
      "step": "10",
      "kickoff_line_range": [703, 703],
      "kickoff_quote": "The\n   branch is not optional",
      "skill_quote": "SEARCH EVIDENCE — closest coverage found is the success-criteria checkbox at line 468: '- [ ] Review scope determined explicitly (file list, diff base, or sprint branch — never guessed)'. Greps run over agents/code-review-gate.md and references/independent-review.md for `not optional|required input|missing.*branch|fail closed|fail-closed|branch` found the fail-closed rule only at line 317, and it is explicitly scoped single mode ('**3. Determine changed files (single mode):** ... **Fallback:** if neither files nor a computable diff base exists, **fail closed**'). Round mode lists `branch` at line 31 as an input ('`branch` — the phase's integration branch name (used for `git diff` context)') but nowhere states that a round-mode dispatch without `branch` must abort.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 468,
      "reasoning": "The agent's hard fail-closed guard for missing scope lives in the single-mode branch of load_context; round mode only enumerates `branch` as an expected input and then runs `git diff main...<branch>` unconditionally. So the guide's 'not optional' emphasis is compensating for a gate the agent does not enforce — keep the guide text, and consider adding a round-mode fail-closed rule to the agent.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-1",
      "class": "load-bearing-parameter",
      "severity": "low",
      "step": "10",
      "kickoff_line_range": [701, 702],
      "kickoff_quote": "1. Dispatch the code-review-gate agent in round mode against PHASE/, passing all three of its\n   round-mode inputs: phase_dir = PHASE/, round = n, branch = <branch>",
      "skill_quote": "Inputs you receive from the orchestrator in round mode:",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 28,
      "reasoning": "Mode selection among the two modes the agent itself offers, plus the actual argument values (PHASE/, the loop counter n, the branch name) — the agent cannot know any of these. Only the enumeration of the three field names is derivable from lines 29-31, and even that reads as a checklist against a real dispatch failure mode, so keep it.",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-6",
      "class": "load-bearing-parameter",
      "severity": "low",
      "step": "10",
      "kickoff_line_range": [705, 706],
      "kickoff_quote": "and computes stop_loop and next_action\n   itself — read those, never re-derive them.",
      "skill_quote": "8. Compute / verify `stop_loop`: `true` iff `counts.P0 == 0 AND counts.P1 == 0 AND previously_seen_classes is empty (or every class listed there is now resolved per a prior round's fixes.json)`. Otherwise `false`.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 99,
      "reasoning": "The agent computes these but has no way to bind its caller's behavior; 'read those, never re-derive them' is a constraint on the orchestrator, which the agent's own text cannot reach. Keep it. Note the guide then contradicts its own instruction by re-deriving the exit condition at lines 710 and 744-745 (claims 12 and 10).",
      "systemic": false,
      "other_locations": []
    },
    {
      "local_id": "code-review-gate-11",
      "class": "load-bearing-parameter",
      "severity": "low",
      "step": "10",
      "kickoff_line_range": [755, 756],
      "kickoff_quote": "*(only if you want a one-shot look at the current diff, outside the loop — it runs\ncode-review-gate in single mode, so it is never a substitute for round 1)*",
      "skill_quote": "**Single mode** (default — no `round` input given): one-shot review of an explicit file list, a phase's diff, or a branch about to land.",
      "skill_file": "/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-core/agents/code-review-gate.md",
      "skill_line": 21,
      "reasoning": "Conditional gating the operator must judge ('only if you want a one-shot look') plus a warning that this optional side-command does not satisfy the loop's round 1 — a cross-step relationship the agent cannot know. Verified accurate against commands/review.md, which dispatches 'agents/code-review-gate (single mode)'; no drift.",
      "systemic": false,
      "other_locations": []
    }
  ],
  "consistency": "",
  "capped": ""
}

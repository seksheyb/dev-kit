{
  "step": "00",
  "title": "Bootstrap",
  "line_range": [23, 127],
  "blocks": [
    {
      "block_index": 0,
      "line_range": [25, 28],
      "verbatim_text": "./bin/bootstrap-devkit-project.sh myProject      # or: --here, to adopt an existing repo\ncd myProject && claude",
      "assets_invoked": [],
      "surrounding_prose": "Heading is '## 0. Bootstrap (terminal)'. This is a terminal/bash block, run before Claude Code even starts. Immediately after the block: 'Only `dev-kit-core` is enabled at this point. The stack lanes get turned on at step 2, once the SDD says which stack you are on. One lane has to come earlier: `dev-kit-backend`, whose `legacy-modernizer` skill the legacy-entry block below invokes before step 2 exists — its enable block sits with that path, under the same predicate. Nothing else needs an early enable: step 1's `assumption-mapping` and `backlog-grooming` are both `dev-kit-core` assets, already on, and the `dev-kit-product` lane's first entry points are step 12 (compliance) and step 15 (analytics).' Then a bolded transition: '**Then establish governance:**' leads into the next block."
    },
    {
      "block_index": 1,
      "line_range": [39, 43],
      "verbatim_text": "Use the constitution skill to interview me and write docs/global/project/constitution.md.\nThen write docs/global/project/PROJECT.md: what this project is, who it is for, what\nsuccess looks like for milestone 1, and any hard constraints known today.",
      "assets_invoked": ["constitution"],
      "surrounding_prose": "Immediately preceded by the bolded line '**Then establish governance:**'. This is the default/milestone-1 governance block (no conditioning italic tag precedes it, unlike the blocks that follow). The next block is explicitly the milestone-2+ alternative to this one, marked '*(milestone 2+ — the modes change, because the repo is no longer greenfield)*'."
    },
    {
      "block_index": 2,
      "line_range": [46, 53],
      "verbatim_text": "This is milestone <M>, not the first. Use the constitution skill in amend mode against the\nexisting docs/global/project/constitution.md — bump its semantic version and propagate the\namendments to dependent docs; do not re-initialize it from the template. Update\ndocs/global/project/PROJECT.md's success definition to this milestone's. Skip the spec-miner\nand doc-ingest paths below entirely: there is no undocumented legacy pile here and this\nproject's own docs are already current.",
      "assets_invoked": ["constitution"],
      "surrounding_prose": "Preceded by italic conditioning text: '*(milestone 2+ — the modes change, because the repo is no longer greenfield)*'. This block is an alternative to the previous (milestone-1) constitution block, used instead of it on milestone 2+. It explicitly instructs skipping the spec-miner and doc-ingest blocks that follow ('Skip the spec-miner and doc-ingest paths below entirely'). Followed by italic conditioning text for the next block: '*(only if the repo has undocumented existing code)*'."
    },
    {
      "block_index": 3,
      "line_range": [56, 60],
      "verbatim_text": "Add \"dev-kit-backend@dev-kit\": true to .claude/settings.json's enabledPlugins and confirm it\nloaded — the block below needs its legacy-modernizer skill, and step 2 is too late to enable it.\nLeave every other lane plugin off; step 2 picks those from the SDD.",
      "assets_invoked": [],
      "surrounding_prose": "Preceded by italic conditioning text: '*(only if the repo has undocumented existing code)*'. This block enables the dev-kit-backend plugin early specifically so the legacy-modernizer skill (invoked in the next block) is available before step 2 would normally turn lanes on. Followed by another identical conditioning tag '*(only if the repo has undocumented existing code)*' preceding the next (spec-miner/legacy-modernizer) block — the two blocks are meant to run together under the same predicate."
    },
    {
      "block_index": 4,
      "line_range": [63, 76],
      "verbatim_text": "Use the spec-miner skill to reverse-engineer the observed requirements from this codebase in\nEARS format into SPEC/spec.md, then the gate-reverse-engineer agent to promote that into a\nLegacy SDD at docs/global/architecture/SDD.md, a Legacy PRD at docs/global/requirements/PRD.md,\nand retrospective ADRs at docs/global/architecture/adr/NNNN-<slug>.md. Then use the\nlegacy-modernizer skill against that recovered picture: dependency map, service boundaries, risk\nregister, and an incremental migration plan with a rollback trigger and owner per phase. Fold\nthe dependency map and the service boundaries into the Legacy SDD, and record the\nmigration-strategy choice — strangler fig or branch by abstraction; a big-bang rewrite is not\non the menu, legacy-modernizer itself prohibits it — as its own ADR in\nthat same bank. Assessment and plan only at this stage: no facades, adapters, characterization\ntests, or monitoring code, because step 8 builds those and step 3's roadmap is what sequences\nthe migration phases.",
      "assets_invoked": ["spec-miner", "gate-reverse-engineer", "legacy-modernizer"],
      "surrounding_prose": "Preceded by italic conditioning text: '*(only if the repo has undocumented existing code)*' (same predicate as the plugin-enable block immediately above it, which exists to make legacy-modernizer available here). The block itself notes scope limits: 'Assessment and plan only at this stage: no facades, adapters, characterization tests, or monitoring code, because step 8 builds those and step 3's roadmap is what sequences the migration phases.' Followed by italic conditioning text for the next block: '*(only if the repo has a pile of ADRs/PRDs/specs outside canonical SITEMAP paths)*'."
    },
    {
      "block_index": 5,
      "line_range": [79, 92],
      "verbatim_text": "Run a workflow to ingest them. Stage 1: one doc-classifier agent per document, all in\nparallel — give each its FILEPATH and OUTPUT_DIR docs/state/intel/classifications/. Each writes\nits classification JSON there and returns a one-line confirmation and nothing else; no JSON and\nno document contents come back inline, so do not ask for structured output. Stage 2 is a\nbarrier, because synthesis needs the whole set at once: one doc-synthesizer agent, given\nCLASSIFICATIONS_DIR docs/state/intel/classifications/, INTEL_DIR docs/state/intel/,\nCONFLICTS_PATH docs/state/tmp/INGEST-CONFLICTS.md and MODE new, reads that directory and merges\nit under precedence rules into INGEST-CONFLICTS.md plus per-type intel under\ndocs/state/intel/. Two Accepted ADRs that contradict each other are an unresolved blocker —\nreport it, never pick a winner. If there are only a handful of docs, skip the workflow: dispatch\nthe classifiers in one message and run the synthesizer in the next turn, same inputs. The\nwork-list script only earns its keep over a long pile.",
      "assets_invoked": ["doc-classifier", "doc-synthesizer"],
      "surrounding_prose": "Preceded by italic conditioning text: '*(only if the repo has a pile of ADRs/PRDs/specs outside canonical SITEMAP paths)*'. Describes a two-stage workflow: Stage 1 dispatches doc-classifier agents in parallel (one per document), Stage 2 is a barrier — a single doc-synthesizer agent run after all classifications complete. Notes a hard rule: two contradictory Accepted ADRs are an unresolved blocker to report, never resolve by picking a winner. Also notes a small-pile shortcut: skip the formal workflow script and just dispatch classifiers then the synthesizer manually. Followed by italic conditioning text for the next block: '*(only if the repo is greenfield with no code yet, but the doc-ingest step above just ran)*' — i.e. that next block depends on this one having run."
    },
    {
      "block_index": 6,
      "line_range": [95, 100],
      "verbatim_text": "Use the graphify skill on that doc corpus to build docs/state/graphs/graph.json — it ingests\ndocs as well as code, so a pure-docs entry still gets a queryable graph even though cso (and the\ncode-path graphify run below) both skip on a true greenfield entry. Re-run it (or\n`--update`) once this milestone's code exists so the graph doesn't go stale.",
      "assets_invoked": ["graphify"],
      "surrounding_prose": "Preceded by italic conditioning text: '*(only if the repo is greenfield with no code yet, but the doc-ingest step above just ran)*', meaning this block's predicate depends on the doc-ingest block immediately before it having executed. Notes that cso and the code-path graphify run (the following block) both skip in a true greenfield entry, so this is the only graph-building path available for a pure-docs greenfield case. Instructs re-running (or `--update`) once code exists later so the graph does not go stale. Followed by italic conditioning text for the next block: '*(only if the repo has code — a legacy/inherited entry, or any milestone after the first)*' — mutually-exclusive alternative to this block."
    },
    {
      "block_index": 7,
      "line_range": [103, 115],
      "verbatim_text": "Run two skills here, in this order, one after the other — they are skills that load into this\nsession rather than agents you can fan out, so there is no dispatching both in one message:\ngraphify mandates its own Agent-tool fan-out for semantic extraction, and cso runs its phases\ninline. First, use the graphify skill to build docs/state/graphs/graph.json, or run it with\n`--update` for an incremental re-extract if a graph already exists. If graphify fails, note it\nand continue — it is an optimization, not a gate. Then use the cso skill for a full security\nbaseline into docs/milestones/<M>/reports/security/ — full, no flags, never --diff: at milestone\n2+ the repo carries every prior milestone's shipped code, and that accumulated surface (stale\ndependencies, drifted CI config, secrets leaked since the last full sweep) is exactly what step\n12's --diff pass is scoped too narrowly to catch. This baseline is what planner's threat-model\nstep and sdd-review-cto both consult later, so it comes before step 2.",
      "assets_invoked": ["graphify", "cso"],
      "surrounding_prose": "Preceded by italic conditioning text: '*(only if the repo has code — a legacy/inherited entry, or any milestone after the first)*' — the code-bearing alternative to the previous pure-docs-greenfield block. Explains the two skills must run sequentially, not dispatched together, since both load into the current session rather than being fanned out as agents. Notes graphify failure is non-fatal ('an optimization, not a gate') but cso must run 'full, no flags, never --diff' at this stage, contrasted with step 12's narrower --diff pass. States this baseline feeds planner's threat-model step and sdd-review-cto later. Followed by an unconditioned block (context-save) — no italic tag precedes it, implying it always runs."
    },
    {
      "block_index": 8,
      "line_range": [117, 120],
      "verbatim_text": "Use the context-save skill to checkpoint this boundary — what is done, what got decided, and\nwhat is next.",
      "assets_invoked": ["context-save"],
      "surrounding_prose": "No conditioning italic tag precedes this block — it follows directly after the graphify/cso block, and appears to run unconditionally at the end of step 0, regardless of which of the preceding conditional branches were taken. Followed immediately by the final block in this step, a bare `/clear` command."
    },
    {
      "block_index": 9,
      "line_range": [122, 124],
      "verbatim_text": "/clear",
      "assets_invoked": [],
      "surrounding_prose": "Follows directly after the context-save block with no conditioning text — appears unconditional, the final action of step 0. Followed by a horizontal rule '---' marking the end of the Bootstrap section, then '## 1. Requirements & product framing' begins the next step."
    }
  ],
  "notes": "The slice boundary (line 127) does not cut any fence in half; line 127 is a blank line right after the closing '---' rule, and step 1's heading begins at line 128, outside this slice. All ten fenced blocks in the range are complete. Rejected non-asset mentions inside these blocks per the trap rule: none of the four listed trap phrases (lane skill / core skills / bare command / test-design skill) appear in this line range. Note that 'legacy-modernizer' is named both in the plugin-enable block (56-60, referenced but not invoked there) and invoked for real in the spec-miner block (63-76); it is only listed under assets_invoked for the latter."
}

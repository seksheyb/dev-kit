{
  "asset": "retro",
  "verdicts": [
    {
      "claim_id": "retro-1",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "retro.md line 5: \"Parse `$ARGUMENTS` for an optional window (`24h`, `14d`, `30d`) or `compare` (default: last 7 days).\"",
      "reasoning": "Verified KICKOFF.md lines 1328-1329 read exactly: '**Retro — set the window to the milestone.** Bare, this command retrospects the last 7 days, so a milestone that ran longer gets a retro covering only its final week.' This does restate the command's own documented default (7 days), but it does so only as the rationale clause motivating the override instruction that immediately follows (line 1329 onward: 'Pass the actual span'). The rubric explicitly carves out 'emphasis of something the skill makes optional' as load-bearing, not duplication, with low severity — that is exactly this case: the command already documents the 7-day default and the `<Nd>` override slot at line 5, and the guide is not teaching new methodology, just flagging why the operator must act. Confirmed as load-bearing-parameter at low severity."
    },
    {
      "claim_id": "retro-2",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "medium",
      "evidence": "retro.md line 5: \"Parse `$ARGUMENTS` for an optional window (`24h`, `14d`, `30d`) or `compare` (default: last 7 days).\"",
      "reasoning": "Verified KICKOFF.md lines 1329-1333 read exactly: 'Pass the actual span:' followed by a fenced code block containing '/dev-kit-core:retro <Nd>'. The command (retro.md, has_references: no, only 9 lines) defines the window argument's accepted formats and its default, but has no way to know how long any given milestone actually ran. `<Nd>` is a per-invocation placeholder the operator must compute and substitute from the milestone's actual span — this matches the rubric's explicit 'placeholders / per-milestone substitutions' example of load-bearing content, not duplication of the skill's internal methodology, gates, or output format. Confirmed as load-bearing-parameter; medium severity is reasonable since this is the operative instruction (vs. claim retro-1's supporting rationale) and omitting it would cause the command to silently use the wrong window for any milestone longer than 7 days."
    }
  ]
}

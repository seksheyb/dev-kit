# growth-loops

- **kind**: skill
- **file**: `/home/ubuntu/skillsproject/dev-kit/plugins/dev-kit-product/skills/growth-loops/SKILL.md`
- **file_lines**: 95
- **has_references**: no
- **complexity**: low
- **invocation_count**: 1
- **invoked by steps**: 15

---

## Invocation 1 — step 15 (Operate, retrospect, close), block 5, lines 1343-1351

### verbatim_text

```text
Close the loop back to step 1's assumption bank with real evidence, using the product lane.
Run these in order, each feeding the next: ab-test-analysis on any experiment this milestone
ran, then cohort-analysis on retention and behavior by cohort, then growth-loops on which loops
actually compounded. Then go back to the assumptions recorded at step 1 and mark each one
validated, invalidated, or still-unknown against what the data now shows, writing the result
where step 1 recorded them. This is the input to milestone 2's scope — without it the next
milestone re-runs on the same unvalidated guesses.
```

### surrounding_prose

Conditioned by the italic line immediately above the fence: '(only if this milestone shipped user-facing changes with usage data behind them)'. The three named analyses must run in the stated order, each feeding the next, then the result is written back to where step 1 recorded the assumption bank — explicitly framed as required input to milestone 2's scope.

---

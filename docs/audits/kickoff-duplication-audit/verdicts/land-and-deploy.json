{
  "asset": "land-and-deploy",
  "verdicts": [
    {
      "claim_id": "land-and-deploy-1",
      "confirmed": true,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "SKILL.md:46 — \"If CLAUDE.md has no `## Deploy Configuration` section, run this wizard before the first deploy (also offer it any time the user asks to \\\"set up deploys\\\").\"",
      "reasoning": "Verified on disk: the skill's only timing constraint is 'before the first deploy', and it explicitly permits standalone invocation. KICKOFF.md:1124-1127 pulls the wizard forward to step 13 (heading at KICKOFF.md:1073 '## 13. Ship') so that it does not fire during the land-and-deploy run at the end of step 14 (heading at KICKOFF.md:1177 '## 14. Document'; the land-and-deploy invocation is at KICKOFF.md:1270). The pipeline-step placement and the 'while I am sitting here to answer it' interactivity-scheduling rationale are facts about the surrounding 15-step pipeline that the skill cannot know. Load-bearing ordering constraint. Keep."
    },
    {
      "claim_id": "land-and-deploy-2",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "SKILL.md:46 — \"If CLAUDE.md has no `## Deploy Configuration` section, run this wizard before the first deploy\"; interactivity at SKILL.md:21 (\"**First-run setup (see below)** — confirm the detected deploy infrastructure once\") and SKILL.md:78 (A/B/C confirmation prompt).",
      "reasoning": "Verified: KICKOFF.md:1125 'It is interactive and it fires only when CLAUDE.md has no `## Deploy Configuration` section' restates the skill's own trigger sentence (SKILL.md:46) plus its own interactivity (SKILL.md:21, 78). It is additionally restated inside the KICKOFF itself — the operator gate at KICKOFF.md:1121-1122 already reads '*(only if CLAUDE.md has no `## Deploy Configuration` section — check that now, not at deploy time)*', so the in-prompt sentence duplicates both the skill and the guide's own conditional. It survives only as the antecedent for the load-bearing timing rationale in claim 1, so it is the least removable duplication in this block — hence low, not medium. I also confirm the claim's secondary observation: the restated condition is incomplete relative to SKILL.md:88, which re-runs the first-run validation when the Deploy Configuration section or the deploy workflow files have changed, a second firing condition the KICKOFF's 'fires only when' wording excludes."
    },
    {
      "claim_id": "land-and-deploy-3",
      "confirmed": false,
      "final_class": "load-bearing-parameter",
      "final_severity": "low",
      "evidence": "SKILL.md:82 — \"1. `gh auth status` — not authenticated → **STOP** (\\\"run `gh auth login`\\\").\" — but this line sits under '## Step 1: Pre-flight' (SKILL.md:80), NOT under '## First-run setup (one-time deploy configuration)' (SKILL.md:44-78).",
      "reasoning": "The claim is backwards about which part of the skill is in scope. The step-13 invocation runs the first-run wizard standalone, and the wizard section (SKILL.md:44-78) contains no `gh auth status` check anywhere — steps 1-4 of the wizard are platform-config-file detection, platform-specific setup, persist-to-CLAUDE.md, and verify/confirm. The `gh auth status` gate belongs to Step 1 Pre-flight, which only runs on an actual deploy invocation at step 14. So KICKOFF.md:1129-1130 is not re-deriving a gate the skill would otherwise enforce in this invocation; it is hoisting a check from a later phase into an earlier, wizard-only run, which is exactly the ordering/scheduling class the discipline calls load-bearing. The tail clause naming `gh auth login` as the remedy does echo SKILL.md:82, but it is one-clause rationale attached to a genuinely additive instruction, nowhere near medium severity. Refuted as classified; the text should be KEPT."
    },
    {
      "claim_id": "land-and-deploy-4",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "SKILL.md:50 — \"Check for platform config files: `fly.toml` (Fly.io), `render.yaml` (Render), `vercel.json`/`.vercel` (Vercel), `netlify.toml` (Netlify), `Procfile` (Heroku), `railway.json`/`railway.toml` (Railway). Check `.github/workflows/*.yml` for deploy/release/production/cd workflows and, separately, staging workflows.\" Fallback branch at SKILL.md:58 — \"**Nothing detected:** ask — how are deploys triggered ...\"",
      "reasoning": "Verified line-by-line. KICKOFF.md:1130-1133 reproduces the skill's detection list one-for-one and in the same order — fly.toml, render.yaml, vercel.json or .vercel, netlify.toml, Procfile, railway.json or railway.toml, then the .github/workflows/ directory — and 'ask me directly for anything you cannot detect' is the skill's 'Nothing detected: ask' branch at SKILL.md:58. This is the skill's own wizard step 1 methodology, not a parameter: the prompt supplies no path, filename, or condition the skill lacks. Severity downgraded from high to medium: the duplication is real and drift-prone (a new platform added to SKILL.md:50 would silently strand this list), but the block is three lines and the skill still performs its own detection regardless, so a stale list degrades the operator prose rather than the behavior."
    },
    {
      "claim_id": "land-and-deploy-5",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "SKILL.md:62 — \"Find-and-replace the `## Deploy Configuration` section or append it:\" followed by the fenced template at SKILL.md:64-74, which fixes exactly eight fields in exactly this order: Platform, Production URL, Staging URL, Deploy workflow, Deploy status command, Merge method, Project type, Post-deploy health check.",
      "reasoning": "Verified by reading the fenced block: the eight field names and their order in KICKOFF.md:1134-1136 match SKILL.md:64-74 exactly. Re-listing a skill's own output template is the canonical duplication pattern named in the classification rules ('the prompt restates output format / section structure the skill's template already fixes'). The section name `## Deploy Configuration` and the CLAUDE.md destination are also both fixed by SKILL.md:46 and 62, so nothing here is an invocation argument. Severity medium rather than high: three lines, no behavioral effect, but a genuine drift surface if the template gains or loses a field."
    },
    {
      "claim_id": "land-and-deploy-6",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "medium",
      "evidence": "SKILL.md:78 — \"Try the health-check URL (`curl -sf {url} -o /dev/null -w \\\"%{http_code}\\\"`) and the status command; report results (failures are warnings, not blockers — the config is still useful). Show the user a validation table — platform, app, prod URL, command results, staging detection, what will happen on a deploy, merge method — and ask: A) That's right, let's go; B) Something's off (they explain, you adjust); C) Reconfigure more carefully first.\"",
      "reasoning": "Verified: every clause of KICKOFF.md:1136-1138 maps onto wizard step 4 at SKILL.md:76-78, in the same order — curl the health-check URL, run the status command, show the validation table, obtain explicit confirmation before treating it as done, and 'A failing check is a warning, not a blocker' which is SKILL.md:78's 'failures are warnings, not blockers'. This is the skill's own internal phase restated; the prompt adds no URL, no command, no condition the skill lacks. Confirmed. Severity medium — largest single methodology restatement in the block, but the skill enforces all of it unaided."
    },
    {
      "claim_id": "land-and-deploy-7",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "SKILL.md:78 (final third of the line) — \"Never print full API keys or tokens while doing any of this.\"",
      "reasoning": "Verified character-for-character: KICKOFF.md:1138-1139 and SKILL.md:78 carry the identical sentence, same wording, same punctuation, including the trailing 'while doing any of this' which only makes sense relative to the wizard's four steps. This is decisive evidence that the KICKOFF block was authored by copying SKILL.md's first-run-setup section rather than written independently, which is the finding's real value. Severity downgraded to low as a removal target: it is one sentence, it is a hard rule the skill already enforces at SKILL.md:78, and restating a safety rule costs nothing behaviorally."
    },
    {
      "claim_id": "land-and-deploy-8",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "SKILL.md:3 (frontmatter description) — \"Merge a PR, wait for CI and the deploy, and verify production health — with a readiness gate before the irreversible merge and revert as the escape hatch at every failure point.\" And SKILL.md:10 — \"This skill picks up where `/ship` left off.\"",
      "reasoning": "Verified: 'revert as the escape hatch at every failure point' in KICKOFF.md:1270-1272 is word-for-word from the frontmatter description at SKILL.md:3, and 'picks up where ship left off' is SKILL.md:10 verbatim. The phase chain 'readiness gate → merge → poll the deploy platform → verify production health' summarizes SKILL.md Steps 3-7. The overlap is not incidental phrasing — it is copied text — so the class holds. Severity dropped from medium to low: this is one sentence of orienting prose in the invocation with zero behavioral effect, and even if the skill's phase list drifted, the agent would still simply run the skill."
    },
    {
      "claim_id": "land-and-deploy-9",
      "confirmed": true,
      "final_class": "redundant-restatement",
      "final_severity": "low",
      "evidence": "SKILL.md:42 — \"**If the platform is GitLab or unknown, STOP:** \\\"GitLab support for land-and-deploy isn't implemented yet — the merge-queue and CI-polling logic below is GitHub-specific. Run `/ship` to create the MR, then merge manually via the GitLab web UI.\\\" This skill only proceeds on GitHub.\" Detection itself at SKILL.md:31-33.",
      "reasoning": "Verified: the skill detects the platform (SKILL.md:31-33), stops on GitLab or unknown, and already directs the user to merge manually — so KICKOFF.md:1272-1273's 'GitHub only — on GitLab or an unrecognized platform it stops and hands off to a manual merge' restates an existing hard gate verbatim in substance, including the 'or unknown/unrecognized' disjunct. Class holds. Severity low, not medium, because one clause of this span is genuinely load-bearing: 'which is the correct outcome, not a failure' tells an agent running an unattended 15-step pipeline not to treat the STOP as a pipeline failure or work around it — framing the skill cannot supply because it does not know it is being driven by a pipeline. The duplicated half should be cut; that clause must survive."
    }
  ]
}
